spring.application.name=e-auction-backend
server.port=19090
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${DB_URL:jdbc:postgresql://192.168.0.104:5432/app_db}
spring.datasource.username=${DB_USERNAME:app_data}
spring.datasource.password=${DB_PASSWORD:8961071912}
logging.level.org.springframework.messaging=DEBUG
## JPA Details
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
management.endpoints.web.exposure.include=*
jwt.secret=${JWT_SECRET:Hh8gKQhwfDktN7Kfr7IvRtwsgV3gBvY48YfUhRFjXWI}
spring.threads.virtual.enabled=true
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:4200,http://localhost:3000}

## Zoho SMTP Details
spring.mail.host=${SMTP_HOST:XXXXXXXXXXX}
spring.mail.port=${SMTP_PORT:XXX}
spring.mail.username=${SMTP_USERNAME:XXXXXXXXXXXXXX}
spring.mail.password=${SMTP_PASSWORD:XXXXXXXX}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

## Apache FreeMarker Details
spring.freemarker.template-loader-path=classpath:/templates/
spring.freemarker.suffix=.ftl